<%= render 'layouts/header' %>
<div class="container">
	<% if @book_edit.errors.any? %>
	    <h2><%= @book_edit.errors.count %> errors prohibited this obj from being saved:</h2>
	    <% @book_edit.errors.full_messages.each do |msg| %>
	    	<li><%= msg %></li>
	    <% end %>
	<% end %>
	<div class="row">
		<div class="col-md-6 col-md-offset-3" style="padding: 0 15px;">
			<h1 style="margin: 20px 0 10px 0;">Editing Book</h1>
			<%= form_for(@book_edit) do |f| %>
			    <div class="row" style="margin-bottom: 10px;">
			        <label style="margin-bottom: 5px;">Title</label>
			        <%= f.text_field :title, {class: "col-xs-12"} %>
			    </div>
			    <div class="row" style="margin-bottom: 10px;">
			    	<label style="margin-bottom: 5px;">Opinion</label>
			    	<%= f.text_area :body, {class: "col-xs-12"} %>
			    </div>
			    <input type="submit" name="commit" value="Update Book" class="btn btn-primary col-xs-12" data-disable-with="Update Book">
		    <% end %>
		    <%= link_to "Show",book_path, {class: "text-muted"} %>
		    <span>|</span>
		    <%= link_to "Back",books_path, {class: "text-muted"} %>
		</div>
	</div>
</div>
<%= render 'layouts/footer' %>