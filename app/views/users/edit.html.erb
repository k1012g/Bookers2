<%= render 'layouts/header' %>
<div class="container">
    <h2 style="margin: 20px 0 10px 0;">Users info</h2>
        <% if @user.errors.any? %>
            <h2><%= @user.errors.count %> errors prohibited this obj from being saved:</h2>
            <% @user.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
            <% end %>
        <% end %>
	<%= form_for(@user) do |f| %>
    <div class="edit-name" style="margin-bottom: 10px;">
        <label>Name</label>
        <%= f.text_field :name %>
    </div>
    <div class="edit-image" style="margin-bottom: 10px;">
        <label>Image</label>
        <%= f.attachment_field :profile_image %>
    </div>
    <div class="edit-intro" style="margin-bottom: 10px;">
    	<label>Introduction</label>
    	<%= f.text_area :introduction %>
    </div>
    <div class="actions" style="margin-bottom: 10px;">
        <input type="submit" name="commit" value="Update User" data-disable-with="Update User">
    <% end %>
    </div>
</div>
<%= render 'layouts/footer' %>